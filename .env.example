# Configuração do Banco de Dados
DB_HOST=studio.rardevops.com
DB_PORT=4202
DB_NAME=postgres
DB_USER=supabase_admin
DB_PASSWORD=sua_senha_aqui
DB_SCHEMA=rarcursos

# Para usar este arquivo:
# 1. Copie para .env: cp .env.example .env
# 2. Edite o .env com suas credenciais reais
# 3. Instale python-dotenv: pip install python-dotenv
# 4. Carregue no início dos scripts: from dotenv import load_dotenv; load_dotenv()